server:
  port: ${SERVER_PORT:8080}

spring:
  datasource:
    url: jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres
    username: postgres.wqillyzrmdbjvxfnckhx
    password: SofwareDevelopemnt
    driver-class-name: org.postgresql.Driver
  servlet:
    multipart:
      max-file-size: ${MAX_FILE_SIZE:10MB}
      max-request-size: ${MAX_REQUEST_SIZE:10MB}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger

# ── JWT Configuration ─────────────────────────────────────────────────────────
jwt:
  secret: ${JWT_SECRET:MyDefaultSuperSecretKeyThatIsAtLeast256BitsLong!!}
  expiration-ms: ${JWT_EXPIRATION:86400000}   # 24 hours

supabase:
  url: ${SUPABASE_URL}
  service-key: ${SUPABASE_SERVICE_KEY}
  storage:
    bucket: ${SUPABASE_STORAGE_BUCKET:data-items}
